# JobSwipe (shukatsu-app) 開発履歴

**作成**: 2026-04-28（Claude Code でのセッションログ `fb6a1d5b...jsonl`（約19MB / 2754行）を解析して再構成）
**期間**: 2026-03-31 〜 2026-04-29
**本番URL**: https://shukatsu-app-iota.vercel.app/
**GitHub**: https://github.com/kazuuekiki/swipejob
**関連プロジェクト**: `C:\Spring 2026\jobswipe-deck\`（英語ピッチ資料）

---

## 1. プロダクト概要と初期方針

最初のメッセージで Kazuki は完全な仕様書を貼り付けた:

> 「Tinder型の新卒就活Webアプリを作成してください。モバイルファーストのWebアプリとして実装してください。登録なしでも企業閲覧可能で、応募時のみ登録必須の設計にしてください。」

**コアコンセプト**:
- 登録なしで企業閲覧・スワイプ可
- 24時間5応募制限
- 応募 → 企業承認でマッチ → マッチ後チャット
- モバイルファースト

開発の早い段階で「いったんログインなしで全部使えるようにできますか」という方針転換があり、`lib/guest.ts` でデフォルト学生（userId=11, profileId=1）を返すゲストモードを採用。これにより認証実装を後回しにしてMVP速度を優先した。

---

## 2. 採用技術と設計判断

| レイヤー | 採用技術 | 採用理由・備考 |
|---|---|---|
| フレームワーク | Next.js 16 (App Router) | モバイルファーストSSR。**破壊的変更が多く** `node_modules/next/dist/docs/` 参照ルールを `AGENTS.md` に明記 |
| 言語 | TypeScript + React 19 | |
| スタイリング | Tailwind CSS v4 | UCLA カラー（ブルー `#2774AE` / ゴールド `#FFD100`）採用 |
| ORM | Prisma 7 | **driver-adapter 必須化**で `prisma.config.ts` 移行に苦労 |
| DB | SQLite（初期） → PostgreSQL（Neon, Vercel連携） | 4/3 にPostgreSQL移行 |
| 認証 | NextAuth 4 + bcryptjs | 応募時のみ要求 |
| ホスティング | Vercel | 4/3 から本番稼働 |

**ファイル編集回数 TOP 5（活動量の指標）**:
1. `app/student/profile/page.tsx`（31回）— プロフィール編集UIの調整が継続
2. `components/SwipeCard.tsx`（25回）— スワイプUX改善
3. `app/student/favorites/page.tsx`（11回）
4. `jobswipe-deck/build.js`（10回）— ピッチ資料生成
5. `app/page.tsx`（12回）— ホーム＋スワイプ統合

---

## 3. 時系列（フェーズ別）

### Phase 1: MVP構築 (3/31 – 4/4)

| 日 | 出来事 |
|---|---|
| 3/31 朝 | 仕様書貼付 → Next.js + Prisma + NextAuth初期化 |
| 3/31 朝 | ゲストモード実装、API認証緩和 |
| 3/31 昼〜夜 | 企業30社シード、入力フォームの白文字バグ修正（ダークモードCSS削除）、スワイプ済企業のlocalStorage制御 |
| 4/1 | 学生プロフィール仕様策定（氏名・学校名・卒業年月必須） |
| 4/3 | GitHub接続、PostgreSQL（Neon）移行、Vercelデプロイ成功 |
| 4/4 | 底部タブナビ、UCLA色テーマ適用、気になるリスト |

**主なつまずき**:
- Prisma 7 の破壊的変更（`url = env("DATABASE_URL")` 廃止 → `prisma.config.ts`）
- ts-node の ESM/CJS 競合 → `tsx` に切替
- ローカル `.env.local` の接続文字列不備で500エラー頻発（Vercel本番では正常）

### Phase 2: UX強化＋実データ統合 (4/5 – 4/13)

UX 8項目を一気に実装:
1. 写真・レジュメ削除ボタン
2. プロフィール編集の「保存忘れ警告」
3. 必須項目バリデーション（氏名・生年月日・自己紹介・自己PRが空なら応募ブロック）
4. プレースホルダー例文
5. **カルチャータグ**（成長志向／体育会系／フラット組織等）
6. **MBTI欄** をプロフィールに追加
7. JobSwipeロゴ（青＆金）配置
8. 進捗トラッカー（応募済 → 書類確認中 → 面接 → マッチ）

**データ拡充**:
- スクレイパーCSV (`prisma/collected.csv`) を経て **企業数 30 → 85 → 89 → 137 社**
- 月給 → 年収範囲チップ表示
- `npm run csv:sync` スクリプト整備

**フィルター機能**:
- 設定ページに「カードの絞り込み設定」セクション
- 都道府県・業界（チップUI）、最低年収スライダー（0〜800万円）
- localStorage に保存しホームに即時反映

**スキーマ追加**:
- `CompanyProfile.culture`、`CompanyProfile.annualSalary`
- `StudentProfile.mbti`

**パフォーマンス最適化**:
- スワイプの重さ → React stateを `useRef` + `requestAnimationFrame` に置換
- API select 絞り込み、ポーリング最適化

**バグ修正の代表例**:
- `culture` カラム未マイグレで本番ローディングが進まない → ビルドスクリプトに `prisma migrate deploy` 追加
- 応募ボタンが下部ナビに隠れる → `bottom-16`
- ハート機能のゲストプロフィール参照エラー → DB動的取得に修正
- **4/13: NextAuth ページがスペイン語表示** とユーザーが報告 → 原因未特定（残課題）

### Phase 3: ピッチ作成と Cowork 移行 (4/14 – 4/29)

開発自体は落ち着き、**事業面の整理にシフト**:

- 4/17–18: 英語パワポ作成、`C:\Spring 2026\jobswipe-deck\JobSwipe_Pitch_Deck.pptx` 完成
- ダミー数字（120+ ES、70%燃え尽き率、¥8.2T TAM等）を全削除し、**「未リリース・ユーザー0・企業提携0」前提**で再構成
- 検証計画（H1–H3）を策定:

| 仮説 | 検証方法 |
|---|---|
| H1: 中堅以下大学生は「自分に合う企業を探す手段」が足りない | 学生 5–10名インタビュー |
| H2: スワイプUXなら心理的コストが下がる | プロトタイプテスト＋既存アプリ比較 |
| H3: 企業側は成功報酬型（¥50K/採用）なら導入しやすい | SMB採用担当 3–5社ヒアリング |

- 4/29: 「co-workで使えるようにして」 → Cowork mode への移行作業（このセッションに引き継ぎ）

---

## 4. 完成済み機能（現在の実装状況）

- Tinder風スワイプUI（フルスクリーン・タッチ＆ドラッグ・RAF最適化）
- 137社の実データシード（CSV同期）
- ゲストモード（ログインなしで全操作）
- 24時間5応募制限
- マッチ＆チャット（5秒ポーリング）
- 学生プロフィール編集（写真・レジュメbase64保存）
- 企業プロフィール編集
- 気になるリスト＆ワンタップ応募
- 底部タブナビ（ホーム/気になる/応募履歴/メッセージ/設定）
- 都道府県・業界・年収フィルター（localStorage）
- UCLAカラーテーマ
- Vercel本番デプロイ + Neon PostgreSQL

---

## 5. 重要な未解決事項・残課題

1. **スペイン語化バグ** — 4/13に NextAuth標準ページが英語/スペイン語表示。原因未特定
2. **ローカルDB接続** — `.env.local` の接続文字列が不安定。Vercel本番のみ確実、ローカルは500エラー多発
3. **Premium UX未実装** — 「3000万円かけたようなオシャレで就活生が使いたくなる感じ」というリクエストに対し、UCLAカラー化までで停止。タイポグラフィ階層、ガラスモーフィズム、マイクロアニメーション未着手
4. **データリセット問題** — `npm run build` で毎回シード実行する設定。本格運用前に解除必須
5. **チャット機能の実データ検証** — 実装済みだがマッチが少なく動作確認が浅い
6. **ピッチ資料のプレースホルダー** — スライド1, 11 に `[Your Name]` 残存
7. **検証計画 H1–H3 の着手** — 学生/企業ヒアリングが未実施

---

## 6. AIアシスタント向け重要メモ

- `AGENTS.md` の指示「これはあなたが知っているNext.jsではない」に従い、Next.js 16関連は必ず `node_modules/next/dist/docs/` を参照する
- スキーマは PostgreSQL だが `prisma/dev.db`（SQLite）が残骸として残っている。DB操作前に接続先を確認
- 本番のシードはビルド時に毎回走るので、データを変更する作業は CSV → seed.ts → ビルド の流れで反映される
- ピッチ資料プロジェクト（`jobswipe-deck/`）は別フォルダ。混同しない

---

## 7. このドキュメントの来歴

このファイルは Claude Code セッションログ `C:\Users\kazu1\.claude\projects\C--Spring-2026\fb6a1d5b-0969-44da-9c7e-e64180e93c66.jsonl` を Cowork mode (Claude) が解析・要約したもの。同じディレクトリの古いログ（`C--GitProject-------JobSwipe---/*.jsonl`）はディレクトリ名に "JobSwipe" を含むが、内容はラグビー集計・マイナビCSV・カオナビ検索など別案件で、JobSwipe開発とは無関係。
